package bank; // Declare that this class belongs to the 'bank' package

// Define a class named 'Account'
class Account {
    // Instance variables (fields) for storing account information
    public String accountNumber;         // To store the account number
    public String accountHolderName;    // To store the name of the account holder
    public double balance;              // To store current balance
    public String accountType;          // To store the type of account (e.g., Savings, Checking)
}
// Define a class named 'Bank' in the same package
public class Bank {
    
}
